class Solution {
public:
	vector<string> wordBreak(string s, unordered_set<string> &dict) {
		int n = s.size();
		vector<vector<string> > mem(n + 1);
		for (int i = 1; i <= n; i++) {
			for (int j = i; j >= 1; j--) {
				if ()
			}
		}
	}
};