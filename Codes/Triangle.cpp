class Solution {
public:
    int minimumTotal(vector<vector<int> > &triangle) {
        if (triangle.empty())
            return 0;
        int n = (int)triangle.size(), m = (int)triangle.size();
        stack<int> levels;
        
        while ()
};
